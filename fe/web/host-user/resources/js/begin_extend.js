/**
 * ${kehaoduo}
 * @author LiuChaoqun
 * @create ${YEAR}-${MONTH}-${DAY} ${TIME}
 **/
var advertiserManager = {
    init: function () {
        $(function(){

        })
    }
};
advertiserManager.init();
